PORT=4000
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/?retryWrites=true&w=majority

# Powers the "Why this pick?" explanation feature. Free tier at
# https://console.groq.com/keys -- leave blank to disable the feature
# (the endpoint returns a clear error instead of failing silently).
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
